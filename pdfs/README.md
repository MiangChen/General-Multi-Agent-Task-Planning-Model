# Local Paper PDFs

Naming convention:

```text
YYYY-MM-DD-algorithm-or-short-title-research-direction.pdf
```

When only year and month are available in the repository metadata, use day `01` so files stay sortable and stable.
